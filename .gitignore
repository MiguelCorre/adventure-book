# Assessment material — not ours to publish
*.pdf
skills/

# Working notes / planning artifacts / tooling
.specs/
.notebook/
CLAUDE.md
AGENTS.md
skills-lock.json
.claude/

# Java / Maven
target/
*.class
!.mvn/wrapper/maven-wrapper.jar

# Application runtime data (H2 file database)
backend/data/

# Uploaded books are runtime data. The four supplied fixtures are already tracked and
# stay tracked (git ignores only untracked files); new uploads are intentionally not versioned.
books/*.json

# Node / Angular
node_modules/
.angular/
dist/
*.log
npm-debug.log*

# Playwright
test-results/
playwright-report/
blob-report/
.playwright/

# IDE
.idea/
*.iml
.vscode/
.settings/
.project
.classpath
nbproject/

# OS
Thumbs.db
.DS_Store
