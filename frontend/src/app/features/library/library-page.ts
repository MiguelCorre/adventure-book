import { Component } from '@angular/core';

/** Placeholder shell for the adventure library. Built out in a later objective. */
@Component({
  selector: 'app-library-page',
  template: `<h1>The Adventure Library</h1>`,
})
export class LibraryPage {}
